"use strict";

var fieldStringLen = function fieldStringLen(value) {
  var length = String(value).length;
  return length === 0 ? 1 : length;
};

module.exports = {
  fieldStringLen: fieldStringLen
};