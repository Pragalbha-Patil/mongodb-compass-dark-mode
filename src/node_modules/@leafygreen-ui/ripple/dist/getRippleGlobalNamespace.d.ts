import { Options } from './utils';
export interface ModuleType {
    '@leafygreen-ui/ripple': {
        registeredRippleElements: WeakMap<HTMLElement, Options>;
        setRippleListener: boolean;
    };
}
export declare type LGWindow = Window & typeof globalThis & {
    __LEAFYGREEN_UTILS__: {
        modules: ModuleType;
    };
};
export declare function getRippleGlobalNamespace(): {
    registeredRippleElements: WeakMap<HTMLElement, Options>;
    setRippleListener: boolean;
} | undefined;
//# sourceMappingURL=getRippleGlobalNamespace.d.ts.map