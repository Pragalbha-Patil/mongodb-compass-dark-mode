import { ReactElement } from 'react';
import PropTypes from 'prop-types';
import { LogoProps, SupportedColors } from './utils';
/**
 * # AtlasLogoLockup
 *
 * React Component that displays the Atlas Logo Lockup.
 *
 * ```
 * <AtlasLogoLockup />
 * ```
 * @param props.color Determines the color of the logo.
 * @param props.height Determines height of the <Logo /> component.
 */
declare function AtlasLogoLockup({ height, className, color, role, 'aria-label': ariaLabel, ...rest }: LogoProps): ReactElement;
declare namespace AtlasLogoLockup {
    var displayName: string;
    var propTypes: {
        height: PropTypes.Requireable<number>;
        color: PropTypes.Requireable<SupportedColors>;
    };
}
export default AtlasLogoLockup;
//# sourceMappingURL=AtlasLogoLockup.d.ts.map