import { ReactElement } from 'react';
import PropTypes from 'prop-types';
import { LogoProps, SupportedColors } from './utils';
/**
 * # MongoDBLogoMark
 *
 * React Component that displays the MongoDB Logo Mark.
 *
 * ```
 * <MongoDBLogoMark />
 * ```
 * @param props.color Determines the color of the logo.
 * @param props.height Determines height of the <Logo /> component.
 */
declare function MongoDBLogoMark({ height, className, color, role, 'aria-label': ariaLabel, ...rest }: LogoProps): ReactElement;
declare namespace MongoDBLogoMark {
    var displayName: string;
    var propTypes: {
        height: PropTypes.Requireable<number>;
        color: PropTypes.Requireable<SupportedColors>;
    };
}
export default MongoDBLogoMark;
//# sourceMappingURL=MongoDBLogoMark.d.ts.map