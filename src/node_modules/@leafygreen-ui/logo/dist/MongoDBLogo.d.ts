/// <reference types="react" />
import PropTypes from 'prop-types';
import { LogoProps, SupportedColors } from './utils';
/**
 * # MongoDBLogo
 *
 * React Component that displays the MongoDB Logo.
 *
 * ```
 * <MongoDBLogo />
 * ```
 * @param props.color Determines the color of the logo.
 * @param props.height Determines height of the <Logo /> component.
 */
declare function MongoDBLogo({ height, className, color, role, 'aria-label': ariaLabel, ...rest }: LogoProps): JSX.Element;
declare namespace MongoDBLogo {
    var displayName: string;
    var propTypes: {
        height: PropTypes.Requireable<number>;
        color: PropTypes.Requireable<SupportedColors>;
    };
}
export default MongoDBLogo;
//# sourceMappingURL=MongoDBLogo.d.ts.map